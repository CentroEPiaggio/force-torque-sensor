# - Config file for the FooBar package
# It defines the following variables
#  FTSENSOR_INCLUDE_DIRS - include directories for FTSensor
#  FTSENSOR_LIBRARIES    - libraries to link against
#  FTSENSOR_EXECUTABLE   - the bar executable

# Compute paths
get_filename_component(FTSENSOR_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(FTSENSOR_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${FTSENSOR_CMAKE_DIR}/FTSensorTargets.cmake")

# These are IMPORTED targets created by FTSensorTargets.cmake
set(FTSENSOR_LIBRARIES FTSensor)